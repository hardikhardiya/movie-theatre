<%= form_with(model: movie_list, local: true) do |form| %>
  <% if movie_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie_list.errors.count, "error") %> prohibited this movie_list from being saved:</h2>

      <ul>
      <% movie_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :movie_list_name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :movie_list_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
